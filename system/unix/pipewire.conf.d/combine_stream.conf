    context.modules = [
        {
            name = libpipewire-module-combine-stream
            args = {
                combine.mode = source
                node.name = "combined_stereo_mix"
                node.description = "Combined Stereo Mix"
                combine.props = {
                    audio.position = [ FL FR ] # Left and Right front channels
                }
                stream.rules = [
                    # Rule for your default audio output (e.g., built-in speakers)
                    {
                        matches = [
                            { media.class = "Audio/Sink" node.name = "alsa_output.pci-0000_03_00.6.analog-stereo" } # Replace with your actual sink name
                        ]
                        actions = {
                            create-stream = {
                                audio.position = [ FL FR ]
                                combine.audio.position = [ FL FR ]
                            }
                        }
                    }
                    # Add more rules for other audio sources if needed
                    # {
                    #     matches = [
                    #         { media.class = "Audio/Source" node.name = "alsa_input.usb-046d_0821_9534DE90-00.analog-stereo" }
                    #     ]
                    #     actions = {
                    #         create-stream = {
                    #             audio.position = [ SL SR ] # Surround Left/Right
                    #             combine.audio.position = [ FL FR ] # Combine into the stereo mix
                    #         }
                    #     }
                    # }
                ]
            }
        }
    ]

